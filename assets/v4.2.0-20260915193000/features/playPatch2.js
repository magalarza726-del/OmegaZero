import { Chess, chooseBotCandidate, resolveBotScheme, chooseTransformMoveOnePly, chooseEnergyMoveOnePly, OPENINGS, getOpening, identifyOpening, openingTreeHtml, openingPreview, nextOpeningSan, MASTER_GAMES, saveDb, addGame, addProblemsFromGame, $, $$, clamp, esc, uciToMove, downloadText, fideGameState, timeoutResult, buildGameStats, announce, playTone } from '../app/deps.js';
import { THEMES } from '../app/config.js';
export const playPatch2={
 renderSetup(){
  this.ensureOpponentConfigs();
  const mode=this.cfg.mode,elo=this.eloForSkill(this.cfg.skill),v=$('#view');
  const opponentCards=this.cfg.opponents.map((o,i)=>{const brain=this.normalizeBrain(o.brain);return `<article class="opponent-card ${i>=this.cfg.simultaneous?'hidden':''}" data-opponent-card="${i}"><header><b>Tablero ${i+1}</b><span>Contrincante ${i+1}</span></header><div class="opponent-grid"><label>Tu color<select id="oppHumanColor${i}"><option value="w">Blancas</option><option value="b">Negras</option></select></label><label>Tipo de COM<select id="oppBrain${i}" data-opp-brain="${i}">${this.comBrainOptions(brain)}</select></label>${this.brainInfoHtml(brain,o.humanColor==='w'?'b':'w')}<label class="scom-field ${brain==='s'?'':'hidden'}">Estilo S-COM<select id="oppStyle${i}"><option value="zero">Zero · defensivo</option><option value="omega">Omega · hiperagresivo</option></select></label><label class="scom-field ${brain==='s'?'':'hidden'}">Nivel Stockfish<input id="oppSkill${i}" data-opp-skill="${i}" type="range" min="1" max="20" value="${o.skill}"><output id="oppSkillOut${i}">${o.skill}/20 · Elo aprox. ${this.eloForSkill(o.skill)}</output></label><label class="advanced-field scom-field ${brain==='s'?'':'hidden'}">Profundidad<input id="oppDepth${i}" type="number" min="1" max="64" value="${o.depth}"></label><label class="advanced-field scom-field ${brain==='s'?'':'hidden'}">Variantes<input id="oppMultiPv${i}" type="number" min="1" max="8" value="${o.multiPv}"></label><label class="opp-color-field ${this.cfg.separateBoardColors?'':'hidden'}">Color del tablero<select id="oppBoardColor${i}">${Object.entries(THEMES).map(([id,t])=>`<option value="${id}">${t.name}</option>`).join('')}</select></label><label class="span2 scom-field ${brain==='s'?'':'hidden'}">Apertura o defensa${this.openingPicker(`oppOpening${i}`,o.opening)}</label></div></article>`}).join('');
  const timePresetValue=this.cfg.clock?`${this.cfg.minutes}+${this.cfg.increment}`:'off';
  const knownTimePresets=['off','3+0','3+2','5+0','10+0','15+10','30+0'];
  const timePresetOptions=[['off','Sin reloj'],['3+0','3 + 0'],['3+2','3 + 2'],['5+0','5 + 0'],['10+0','10 + 0'],['15+10','15 + 10'],['30+0','30 + 0']].map(([value,label])=>`<option value="${value}" ${timePresetValue===value?'selected':''}>${label}</option>`).join('')+(!knownTimePresets.includes(timePresetValue)?`<option value="${timePresetValue}" selected>Personalizado · ${this.cfg.minutes} + ${this.cfg.increment}</option>`:'');
  const wb=this.normalizeBrain(this.cfg.whiteBrain),bb=this.normalizeBrain(this.cfg.blackBrain);
  v.innerHTML=`<section class="page-head"><button data-back>←</button><div><small>CONFIGURAR PARTIDA</small><h1>${mode==='pvp'?'J1 vs J2':mode==='pvc'?'J1 vs COM':'COM vs COM'}</h1>${mode!=='pvp'?'<p>El tablero es único; solo cambia el cerebro conectado a cada bando.</p>':''}</div></section><div class="setup-mode-tabs"><button data-setup-mode="quick" class="${this.cfg.setupMode==='quick'?'active':''}">Configuración rápida</button><button data-setup-mode="advanced" class="${this.cfg.setupMode==='advanced'?'active':''}">Configuración avanzada</button></div><section class="panel form-grid ${this.cfg.setupMode==='quick'?'quick-mode':'advanced-mode'}">
  ${mode==='pvc'?`<label>Partidas simultáneas<input id="simultaneous" type="number" min="1" max="5" value="${this.cfg.simultaneous}"></label><label class="toggle"><input id="separateBoardColors" type="checkbox" ${this.cfg.separateBoardColors?'checked':''}><span>Colores diferentes por tablero</span></label><label class="toggle span2"><input id="independentSimClocks" type="checkbox" ${this.cfg.independentSimClocks?'checked':''}><span>Reloj independiente para cada partida</span></label><small class="span2 sim-clock-help">Cada tablero puede usar S-COM, T-COM o E-COM de forma independiente.</small><div class="simultaneous-configs span2">${opponentCards}</div>`:''}
  ${mode==='cvc'?`<label>COM blancas<select id="whiteBrain">${this.comBrainOptions(wb)}</select></label><label>COM negras<select id="blackBrain">${this.comBrainOptions(bb)}</select></label><div class="span2 cvc-brain-summary"><div>${this.brainInfoHtml(wb,'w')}</div><div>${this.brainInfoHtml(bb,'b')}</div></div><label class="scom-white ${wb==='s'?'':'hidden'}">Estilo S-COM blancas<select id="white"><option value="zero">Zero</option><option value="omega">Omega</option></select></label><label class="scom-black ${bb==='s'?'':'hidden'}">Estilo S-COM negras<select id="black"><option value="zero">Zero</option><option value="omega">Omega</option></select></label><label>Partidas automáticas<input id="repeat" type="number" min="1" max="100" value="${this.cfg.repeat}"></label><label class="toggle"><input id="alternateColors" type="checkbox" ${this.cfg.alternateColors?'checked':''}><span>Alternar colores entre partidas</span></label><label class="advanced-field">Diversidad (%)<input id="diversity" type="range" min="0" max="60" value="${this.cfg.diversity}"><output id="diversityOut">${this.cfg.diversity}%</output></label><label class="toggle"><input id="autoPlay" type="checkbox" ${this.cfg.autoPlay?'checked':''}><span>Rotación automática de turnos</span></label><small class="span2">T-COM y E-COM miran exactamente una semijugada. S-COM usa Stockfish y sus variantes internas.</small>`:''}
  ${mode==='cvc'?`<label class="scom-shared ${wb==='s'||bb==='s'?'':'hidden'}">Nivel Stockfish<input id="skill" type="range" min="1" max="20" value="${this.cfg.skill}"><output id="skillOut">${this.cfg.skill}/20 · Elo aprox. ${elo}</output></label><label class="advanced-field scom-shared ${wb==='s'||bb==='s'?'':'hidden'}">Profundidad<input id="depth" type="number" min="1" max="64" value="${this.cfg.depth}"></label><label class="advanced-field scom-shared ${wb==='s'||bb==='s'?'':'hidden'}">Variantes<input id="multiPv" type="number" min="1" max="8" value="${this.cfg.multiPv}"></label><label class="toggle span2 scom-shared ${wb==='s'||bb==='s'?'':'hidden'}"><input id="independentOpenings" type="checkbox" ${this.cfg.independentOpenings?'checked':''}><span>Aperturas independientes de S-COM</span></label><div id="openingShared" class="span2 scom-shared ${wb==='s'||bb==='s'?'':'hidden'} ${this.cfg.independentOpenings?'hidden':''}"><label>Apertura compartida${this.openingPicker('opening',this.cfg.opening)}</label></div><div id="openingIndependent" class="span2 opening-pair scom-shared ${wb==='s'||bb==='s'?'':'hidden'} ${this.cfg.independentOpenings?'':'hidden'}"><label>Repertorio de blancas${this.openingPicker('whiteOpening',this.cfg.whiteOpening)}</label><label>Repertorio de negras${this.openingPicker('blackOpening',this.cfg.blackOpening)}</label></div>`:''}
  ${mode==='pvp'?`<label class="quick-only span2">Control de tiempo<select id="timePreset">${timePresetOptions}</select></label>`:''}<label class="toggle span2"><input id="clock" type="checkbox" ${this.cfg.clock?'checked':''}><span>Usar reloj</span></label><label class="${mode==='pvp'?'advanced-field':''}">Minutos<input id="minutes" type="number" min="1" max="180" value="${this.cfg.minutes}"></label><label class="${mode==='pvp'?'advanced-field':''}">Incremento (s)<input id="increment" type="number" min="0" max="60" value="${this.cfg.increment}"></label>${mode!=='pvp'?'<button type="button" data-open-models class="span2">⚙ Configurar modelos COM</button>':''}<button id="start" class="primary span2">INICIAR PARTIDA →</button></section>`;
  $('[data-back]').onclick=()=>{this.screen='home';this.render()};
  for(const id of ['white','black'])if($('#'+id))$('#'+id).value=this.cfg[id];
  if($('#whiteBrain'))$('#whiteBrain').value=wb;if($('#blackBrain'))$('#blackBrain').value=bb;
  this.cfg.opponents.forEach((o,i)=>{for(const [field,prefix] of [['humanColor','oppHumanColor'],['style','oppStyle'],['brain','oppBrain'],['boardColor','oppBoardColor']])if($('#'+prefix+i))$('#'+prefix+i).value=o[field]});
  $('#skill')?.addEventListener('input',e=>$('#skillOut').textContent=`${e.target.value}/20 · Elo aprox. ${this.eloForSkill(e.target.value)}`);
  $$('[data-opp-skill]').forEach(x=>x.addEventListener('input',e=>{$(`#oppSkillOut${e.target.dataset.oppSkill}`).textContent=`${e.target.value}/20 · Elo aprox. ${this.eloForSkill(e.target.value)}`}));
  $('#simultaneous')?.addEventListener('input',e=>{const n=clamp(e.target.value,1,5);$$('[data-opponent-card]').forEach((c,i)=>c.classList.toggle('hidden',i>=n))});
  $('#separateBoardColors')?.addEventListener('change',e=>$$('.opp-color-field').forEach(x=>x.classList.toggle('hidden',!e.target.checked)));
  $('#independentOpenings')?.addEventListener('change',e=>{$('#openingShared')?.classList.toggle('hidden',e.target.checked);$('#openingIndependent')?.classList.toggle('hidden',!e.target.checked)});
  $('#timePreset')?.addEventListener('change',event=>{const value=event.target.value,clock=$('#clock'),minutes=$('#minutes'),increment=$('#increment');if(value==='off'){clock.checked=false;return}const [m,inc]=value.split('+').map(Number);clock.checked=true;if(minutes)minutes.value=m;if(increment)increment.value=inc});
  $$('[data-setup-mode]').forEach(button=>button.onclick=()=>{this.captureSetupForm();this.cfg.setupMode=button.dataset.setupMode;this.persistSetupConfig();this.renderSetup()});$('#diversity')?.addEventListener('input',event=>$('#diversityOut').textContent=`${event.target.value}%`);
  $$('[data-opp-brain],#whiteBrain,#blackBrain').forEach(control=>control.addEventListener('change',()=>{this.captureSetupForm();this.persistSetupConfig();this.renderSetup()}));
  $('[data-open-models]')?.addEventListener('click',()=>{this.captureSetupForm();this.persistSetupConfig();this.screen='modelLab';this.render()});
  this.bindOpeningPickers();this.bindLiveSetupPersistence();$('#start').onclick=()=>this.startGameFromForm();
 },
};
